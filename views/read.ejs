<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create User</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">

        <div class="nav mb-10">
            <a class="text-blue-500" href="/">back to home</a>
        </div>

        <h3 class="text-3xl tracking-tighter mb-3">Read User</h3>

        <div class="users flex flex-wrap gap-4">

            <% users.forEach(function(user){ %>

                <div class="user w-64 bg-zinc-800 rounded-lg p-4">
                    <div class="w-full h-52 bg-zinc-500 rounded-lg overflow-hidden">
                        <img class="w-full h-full object-cover" src="<%= user.image%>" alt="">
                    </div>
                    <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
                    <h5 class="text-zinc-600"><%= user.email %></h5>
    
                    <div class="flex mt-10 gap-3">
                        <a class="text-zinc-200" href="">edit user</a>
                        <a class="text-red-600 ml-2" href="/delete/<%= user._id%>">delete user</a>
                    </div>
                </div>
            <% }) %>
            
        </div>
        
    </div>

</body>
</html>